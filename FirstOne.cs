using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FirstOne : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {   // типо пометка пишет привет
        print("Hello world!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
